# RayTracer

Running:

`python ray_tracer.py --width 500 --height 500 "scenes/pool.txt" "output/pool.png"`

The first parameter is the scene file, and the second is the name of the image file to write. Those two are mandatory